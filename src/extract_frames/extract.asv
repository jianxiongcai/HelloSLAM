% read in vedio frames
vedio_path = fullfile('..','..','data','1','Video-2017-12-18-00-08-51_0280.mov');
saving_path = fullfile('..','..','data','1','rgb');
try
    madir(saveing_path);
catch 
    disp('sav')
end
v = VideoReader();
while (hasFrame(v))
    f = readFrame(v);
    % save each indivual frames to file system
    
end